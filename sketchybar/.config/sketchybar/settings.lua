return {
  paddings = 3,
  group_paddings = 5,

  icons = "NerdFont", -- "sf-symbols" needs SF Pro/SF Mono installed

  -- This is a font configuration for SF Pro and SF Mono (installed manually)
  font = require("helpers.default_font"),
}
